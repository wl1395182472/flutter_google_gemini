import 'package:flutter/material.dart';

import 'app.dart';

void main() {
  runApp(const MyApp());
}

//flutter run -d web-server --web-port 8989 --web-hostname 0.0.0.0 --web-renderer html
//flutter build web --release --web-renderer html
